spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/oficina_db?currentSchema=oficina}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  jpa:
    show-sql: true
